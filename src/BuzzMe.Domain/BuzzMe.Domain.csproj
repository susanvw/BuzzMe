<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
  </PropertyGroup>

  <ItemGroup>
    <!-- Rehydration factories (e.g. Board.Rehydrate) are internal so only the Infrastructure
         Mapper that persisted an aggregate can reconstruct it — DEVELOPMENT_GUIDE.md §3/§4. -->
    <InternalsVisibleTo Include="BuzzMe.Infrastructure" />
    <InternalsVisibleTo Include="BuzzMe.Domain.Tests" />
  </ItemGroup>

</Project>
